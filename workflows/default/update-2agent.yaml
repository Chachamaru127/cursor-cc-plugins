# /update-2agent ワークフロー定義
# 既存の2エージェント体制セットアップを更新

phase: update-2agent
description: "既存の2エージェント体制を最新バージョンに更新する（差分マージ対応）"

steps:
  # Step 1: Case-Insensitive でファイルを検出
  - id: detect-existing-files
    skill: ccp-update-2agent-files
    input:
      detect_patterns:
        - pattern: "agents.md"
          standard: "AGENTS.md"
        - pattern: "claude.md"
          standard: "CLAUDE.md"
        - pattern: "plans.md"
          standard: "Plans.md"
      context_from:
        - repo_tree
        - plugin_version
    output:
      variables:
        - detected_files      # { agents: "./AGENTS.md", claude: null, plans: "./plans.md" }
        - needs_normalization # ファイル名を正規化する必要があるか
        - setup_status        # not_installed | current | outdated
        - installed_version
        - plugin_version
    mode: required

  # Step 2: 更新判定
  - id: check-update-needed
    skill: ccp-update-2agent-files
    input:
      variables:
        - setup_status
        - installed_version
        - plugin_version
      force_update: "{{force_option}}"  # --force オプション
    output:
      variables:
        - should_proceed
        - update_reason  # outdated | force | not_needed | not_installed
    mode: required

  # Step 3: バックアップ作成
  - id: create-backup
    skill: ccp-update-2agent-files
    condition: "should_proceed"
    input:
      detected_files: "{{detected_files}}"
      backup_dir: ".cursor-cc-backup-{{timestamp}}"
    output:
      variables:
        - backup_path
        - backup_files
    mode: required

  # Step 4: Plans.md のマージ更新
  - id: merge-plans
    skill: ccp-merge-plans
    condition: "should_proceed && detected_files.plans"
    input:
      existing_file: "{{detected_files.plans}}"
      template: "templates/Plans.md.template"
      variables:
        - project_name
        - date
    output:
      variables:
        - tasks_preserved  # { wip: 2, todo: 5, done: 10, archive: 3 }
        - merge_successful
    mode: required

  # Step 5: AGENTS.md / CLAUDE.md の更新
  - id: update-workflow-files
    skill: ccp-generate-workflow-files
    condition: "should_proceed"
    input:
      templates:
        - templates/AGENTS.md.template
        - templates/CLAUDE.md.template
      variables:
        - project_name
        - date
      overwrite: true
      normalize_filenames: "{{needs_normalization}}"
      old_files: "{{detected_files}}"
    output:
      create_files:
        - AGENTS.md
        - CLAUDE.md
      variables:
        - normalized_files  # リネームされたファイル
    mode: required

  # Step 6: Cursor コマンドの更新（全5ファイル）
  - id: update-cursor-commands
    skill: ccp-setup-2agent-files
    condition: "should_proceed"
    input:
      templates:
        - templates/cursor/commands/start-session.md
        - templates/cursor/commands/project-overview.md
        - templates/cursor/commands/plan-with-cc.md
        - templates/cursor/commands/assign-to-cc.md
        - templates/cursor/commands/review-cc-work.md
      variables:
        - plugin_version
        - date
        - project_name
        - installed_version
      update_mode: true
    output:
      create_files:
        - .cursor/commands/start-session.md
        - .cursor/commands/project-overview.md
        - .cursor/commands/plan-with-cc.md
        - .cursor/commands/assign-to-cc.md
        - .cursor/commands/review-cc-work.md
    mode: required

  # Step 7: Hooks 設定の更新（v0.3.7+）
  - id: update-hooks
    skill: ccp-setup-2agent-files
    condition: "should_proceed"
    input:
      templates:
        - templates/hooks/auto-cleanup-hook.sh
        - templates/.cursor-cc-config.yaml.template
      variables:
        - plugin_version
    output:
      create_files:
        - .claude/scripts/auto-cleanup-hook.sh
        - .cursor-cc-config.yaml
      configure:
        - .claude/settings.json
    mode: optional

  # Step 8: バージョンファイルの更新
  - id: update-version-file
    skill: ccp-setup-2agent-files
    condition: "should_proceed"
    input:
      variables:
        - plugin_version
        - date
        - installed_version
      update_mode: true
    output:
      create_files:
        - .cursor-cc-version
    mode: required

# 成功時の出力
on_success:
  message: |
    ✅ **2エージェント体制を更新しました！** (v{{installed_version}} → v{{plugin_version}})

    ---

    ## 📋 このバージョンの変更点

    **v0.3.7 の新機能:**
    - 🧹 自動整理機能（PostToolUse Hooks）
    - `/cleanup` コマンド追加
    - `.cursor-cc-config.yaml` で閾値カスタマイズ可能

    **v0.3.6 の新機能:**
    - `/start-session` 統合フロー
    - Cursor コマンド 5つに拡充
    - ソロ/2エージェントモード説明追加

    ---

    ## 📝 更新されたファイル

    | ファイル | 内容 |
    |---------|------|
    | AGENTS.md | 構造を更新 |
    | CLAUDE.md | 構造を更新 |
    | Plans.md | タスク保持 + 構造更新 |
    | .cursor/commands/ | 5ファイルに拡充 |
    | .claude/scripts/ | 自動整理スクリプト追加 |
    | .cursor-cc-config.yaml | 設定ファイル追加 |

    ---

    ## 📋 保持されたタスク

    - 進行中: {{tasks_preserved.wip}}件
    - 未着手: {{tasks_preserved.todo}}件
    - 完了: {{tasks_preserved.done}}件
    - アーカイブ: {{tasks_preserved.archive}}件

    ---

    📦 **バックアップ**: `{{backup_path}}`

    💡 問題があればバックアップから復元できます

    🚀 **次のステップ**: Cursor で `/start-session` を実行

# セットアップされていない場合
on_not_installed:
  message: |
    ⚠️ **2エージェント体制がセットアップされていません**

    まず `/setup-2agent` を実行してください。

# 既に最新版の場合
on_current:
  message: |
    ✅ **最新バージョン (v{{plugin_version}}) です**

    強制的に更新する場合は「強制更新して」と言ってください。

# ファイル名正規化の確認
on_normalization_needed:
  message: |
    📝 **ファイル名の正規化が必要です**

    検出されたファイル:
    {{#each detected_files}}
    - `{{this.found}}` → `{{this.standard}}`
    {{/each}}

    正規化しますか？ (y/n)

# エラー時の出力
on_error:
  message: |
    ⚠️ **更新中にエラーが発生しました**

    バックアップが `{{backup_path}}` に保存されています。
    復元する場合は「バックアップから復元して」と言ってください。

    詳細: {{error_message}}
